{
  "name": "ProtoPI",
  "private": true,
  "workspaces": [
    "packages/dev-aids/*",
    "packages/extensions/*"
  ],
  "scripts": {
    "start": "yarn workspace @myorg/package1 start",
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "cleanup:yarn": "yarn cache clean",
    "cleanup:files": "find . -type d \\( -name 'node_modules' -o -name '.vscode-test' -o -name 'dist' -o -name 'out' -o -name 'dist_dev' -o -name 'dist_prod' \\) -exec rm -rf {} + && find . -type f -name 'yarn.lock' -exec sh -c '> {}' \\; && echo 'Cleanup completed!'",
    "cleanup": "concurrently \"yarn cleanup:yarn\" \"yarn cleanup:files\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^4.0.0"
  }
}
